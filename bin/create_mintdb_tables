#!/bin/bash

set -eu

# A hack that works.
schemas="$(dirname "$0")/../model/schemas"/*.mint.*.go


perl -ne 'print if /`/.../`/' "$(dirname "$0")/../model/schemas/schemas.go" | sed '1d;$d'
for i in $schemas; do
    perl -ne 'print if /`/.../`/' "$i" | sed '1d;$d'
done
