package model

// Address represents a Stellar address (or public key) (using the accout ID
// format).
type Address string

// Seed represents a seed associated with an address.
type Seed string

// Signature represents a signature encoded in base64.
type Signature string
